# Mini Kernel 指令说明书（Shell 命令手册）

> **适用版本**：v2.7.1（三部分分离）
> **就近在线参考**：在串口终端输入 `help` 可查看全部命令缩写；本说明书为完整版。
>
> **三个部分的关系**：
> - **mini-kernel（纯内核库）**：只提供基础命令（下表「基础命令」一节），不含任何 RP2040 硬件命令。
> - **rp2040demo（RP2040 Demo 工程）**：除基础命令外，还包含全部 RP2040 专属命令（超频/多核/MSC/文件系统/固化/OLED 验证等），**并带展示任务**（OLED FPS + 呼吸 LED + GP15 PWM）。
> - **rp2040system（RP2040 系统工程）**：与 rp2040demo **完全同命令集**，但**无展示任务**（`OS_CFG_SHOW_DEMO=0`）。其余全部 Shell 命令（fs / msc / ovclk / mcore / ipc / bootscript / vtest / reboot / bootlog ...）与功能完全一致。

---

## 〇、RP2040 时钟树（超频相关，先看这里）

RP2040 不同外设吃不同时钟源，超频只改 CPU 计算速度，**周期类功能不受影响**：

| 外设 / 功能 | 时钟源 | 超频到 250MHz 时的行为 |
|---|---|---|
| CPU 指令执行、渲染算术 | `clk_sys` | ✅ 变快（约 2 倍） |
| PWM 载波频率（GP25/GP15）、I2C/SPI/UART 波特率 | `clk_peri` | 不变 —— 超频代码把 `clk_peri` 钳回 125MHz |
| 内核 tick、`task_sleep`、呼吸灯/心跳/台阶间隔、`time_us_32` | `clk_ref`（= XOSC 12MHz，固定） | 不变 —— `clk_ref` 固定，不随 `clk_sys` 变 |
| USB | `pll_usb`（48MHz，独立） | 不变 |

**结论**：超频后 PWM 载波、呼吸灯闪烁、GP15 台阶间隔、心跳周期**都不会变短**，只有纯计算更快。

---

## 一、基础命令（mini-kernel 纯库 + rp2040demo 均可用）

| 命令 | 用法 | 说明 |
|---|---|---|
| `help` | `help [cmd]` | 打印帮助信息；带参数可查看单条命令的详细用法 |
| `ps` | `ps` | 列出所有任务（ID / 状态 / 栈基址 / 栈大小 / 栈是否溢出） |
| `heap` | `heap` | 打印内核堆空闲字节数与最大空闲块 |
| `tick` | `tick` | 打印当前系统 tick 值（@`OS_CFG_TICK_HZ` Hz） |
| `version` / `ver` | `version` | 打印内核版本字符串 |
| `suspend` | `suspend <id>` | 挂起指定 ID 的任务（用 `ps` 查 ID） |
| `resume` | `resume <id>` | 恢复指定 ID 的被挂起任务 |
| `kill` | `kill <id>` | 销毁指定 ID 的任务（不可恢复） |
| `clear` / `cls` | `clear` | 清屏并将光标移至左上角 |
| `led` | `led on\|off\|toggle` | 控制板载 LED（RP2040 为 GPIO25） |
| `gpio` | 见下方小节 | GPIO 子命令（初始化/读/写/翻转） |
| `i2c` | 见下方小节 | I2C 主机子命令（扫描/读写/寄存器访问） |
| `syscalls` | `syscalls` | 列出系统调用契约表 |

> 说明：`led / gpio / i2c` 依赖 `OS_CFG_PERIPH_SERVICE=1`（仅在 rp2040demo 中启用）；纯内核库默认关闭，输入会提示 `OS_CFG_PERIPH_SERVICE=0`。

### `gpio` 子命令

```
gpio help
gpio init  <pin> in                   # 浮空输入
gpio init  <pin> out   [0|1]          # 推挽输出，可选初始电平
gpio init  <pin> out_od [0|1]         # 开漏输出，可选初始电平
gpio init  <pin> af <af_num>          # 复用功能（0=SPI,1=UART0,2=UART1,3=I2C0,4=I2C1,5=SIO,6=PWM...）
gpio read  <pin>                      # 读引脚电平 -> 0 / 1
gpio write <pin> <0|1>                # 写输出电平
gpio toggle <pin>                     # 翻转输出电平
```

示例：`gpio init 25 out 1`（GPIO25 推挽输出，初始高电平点亮板载 LED）。

### `i2c` 子命令

```
i2c init <bus> <sda> <scl> <hz>       # 初始化并配置 I2C 引脚（bus=0|1，典型 hz=100000/400000/1000000）
i2c scan <bus>                        # 扫描 7-bit 地址 0x08..0x77
i2c wr   <bus> <addr> <b1> [..bn]     # 裸写 N 字节
i2c rd   <bus> <addr> <len>           # 裸读 len 字节
i2c cmds <bus> <addr> <c1> [..cn]     # SSD1306 风格：0x00 前缀 + N 个命令字节
i2c fill <bus> <addr> <byte> <cnt>    # SSD1306 风格：0x40 前缀 + cnt×byte（GDRAM 填充）
i2c memwr <bus> <addr> <reg> <b1> [..] # 设备寄存器写（mem 16-bit）
i2c memrd <bus> <addr> <reg> <len>     # 设备寄存器读（mem 16-bit）
```

示例：`i2c init 0 4 5 100000`（I2C0，SDA=GP4/SCL=GP5，100kHz）；`i2c w 0 0x3C 0x00 0xAF`（向 OLED 0x3C 写 2 字节）。

---

## 二、RP2040 专属命令（仅 rp2040demo）

> 以下命令依赖 `OS_CFG_PORT_RP2040=1`，纯内核库不编译。

### 2.1 任务控制（Linux 风格）

| 命令 | 用法 | 说明 |
|---|---|---|
| `jobs` | `jobs` | 列出用户创建的后台任务（类似 Linux jobs，跳过内核任务） |
| `vtest` | `vtest start [bus] [addr]\|status\|stop` | 三任务嵌套调度稳定性验证（见下方小节） |

`Ctrl+C`：中断后台任务（如正在运行的 `vtest`）并清空当前输入行。

#### `vtest` 子命令

```
vtest start [bus] [addr]   # 启动 VT1(LED心跳)/VT2(OLED刷新)/VT3(压力+suspend/resume)
vtest status               # 查看三任务计数器 + 栈/堆占用
vtest stop                 # 销毁三任务，恢复 LED OFF
```

### 2.2 固化指令（bootscript）—— 掉电保留

| 命令 | 用法 | 说明 |
|---|---|---|
| `save` | `save <any command...>` | 把任意命令固化到 Flash，下次开机自动执行（不立即执行） |
| `unsave` | `unsave <idx>\|all` | 删除固化命令：按序号或一键清空 |
| `list` | `list` | 列出所有已固化命令（Flash 双备份 + CRC） |
| `boot` | `boot exec\|flash_test\|status` | 固化指令子系统：`exec`=立即执行全部；`status`=查看上次回放结果；`flash_test`=B 线路 SPI 双备份自检 |
| `factory_reset` | `factory_reset [confirm]` | 出厂重置：擦除所有持久化数据（bootscript + 末尾保留区），保留内核固件 |
| `reboot` | `reboot` | 软复位系统（重现开机画面 + 应用固化超频/多核） |
| `bootlog` | `bootlog` | 开机日志回放诊断（captured/ replayed / capturing / dtr） |

> `!` 前缀语法糖：`!<命令...>` 先执行该命令，成功后再固化到 Flash。
> 例：`! i2c init 0 4 5 100000` 会立即执行并永久保存该初始化命令。

### 2.3 超频 / 多核（v2.4）

| 命令 | 用法 | 说明 |
|---|---|---|
| `ovclk` | `ovclk list\|set\|try\|mcore\|get\|save\|unsave\|reset` | 超频 / 多核固化控制 |

```
ovclk list                 # 列出预设档位（125/250/375/500 MHz），也支持任意 100..500 MHz
ovclk set <tier|MHz>       # 设置待固化频率（仅 RAM，需 save + reboot 生效）
ovclk try <tier|MHz>       # 运行时临时切频（不固化，reboot 恢复）
ovclk mcore <0|1>          # 设置待固化多核标志（0=单核 1=双核）
ovclk get                  # 查看当前实际主频 + 已固化配置
ovclk save                 # 把频率+多核写入 Flash（掉电保留）
ovclk unsave | reset       # 擦除固化区，重启恢复 单核 + 125MHz
```

### 2.4 多核调度测试

| 命令 | 用法 | 说明 |
|---|---|---|
| `mcore` | `mcore status\|demo\|stop` | 在 core1 上创建心跳任务并查看多核调度 |

```
mcore status   # 查看 core0/core1 当前任务 + core1 计数
mcore demo     # 启动 core1 + 在 core1 创建每 100ms 自增的心跳任务
mcore stop     # 销毁 core1 心跳任务
```

### 2.5 USB 大容量存储（MSC）与文件系统

| 命令 | 用法 | 说明 |
|---|---|---|
| `msc` | `msc mount\|eject\|status\|format` | USB 大容量存储控制 |
| `pwd` | `pwd` | 打印当前目录 |
| `cd` | `cd [path]` | 切换当前目录（绝对或相对，默认 `/`） |
| `ls` | `ls [path]` | 列出目录内容 |
| `mkdir` | `mkdir <dir>` | 创建目录（需先 `msc eject`） |
| `rmdir` | `rmdir <dir>` | 删除**空**目录（需 `msc eject`） |
| `rm` | `rm <file\|empty-dir>` | 删除文件或空目录（需 `msc eject`） |
| `cat` | `cat <file>` | 打印文件内容（无二进制过滤） |

> **MSC 互斥规则**：
> - `msc mount`：让电脑看到 U 盘（Shell 进入只读）。
> - `msc eject`：收回 U 盘（Shell 恢复读写，可 `mkdir/rm`）。
> - `msc format`：格式化为 FAT16（空盘或损坏时使用）。
> - 首次上电空盘会自动 `f_mkfs` 格式化为 FAT16。

### 2.6 双核共享内存 IPC（v2.6 新增）

| 命令 | 用法 | 说明 |
|---|---|---|
| `ipc` | `ipc start` | 拷贝 Core1 镜像到 SRAM 并拉起 worker（WFE 事件驱动） |
| | `ipc stop` | 复位 Core1，回到 READY |
| | `ipc status` | 查看 ctrl 状态 / 心跳存活 / 帧统计 / 内存映射 |
| | `ipc bench [n]` | n 帧（默认 16）A/B 乒乓实测往返时延与吞吐 |
| | `ipc selftest` | 三阶段自检：心跳 + 单帧 + 4 帧交替乒乓 |
| | `ipc fifo` | SIO FIFO 探针：寄存器状态/排空/echo 往返/core1 断电对照（排障用，输出在 TTL/UART0） |

```
ipc start        # 拉起 Core1（与 mcore demo 互斥，会先复位 core1）
ipc status       # state=RUNNING alive=Y hb=1000->1012 ...
ipc bench 64     # 64 帧乒乓：avg/min/max 往返时延 + KB/s 吞吐
ipc selftest     # [1/3]心跳 [2/3]单帧 [3/3]乒乓 全 PASS
ipc fifo         # FIFO 健康度探针（hex 输出走 TTL 串口）
ipc stop         # 复位 Core1
```

> **架构说明**：
> - Core0 跑内核 + 传感器采集；Core1 裸循环（SRAM 镜像 @0x20019000，不开中断）。
> - 数据通道：2×16KB 输入 + 2×16KB 输出乒乓缓冲（0x20020000 / 0x20028000）。
> - 同步：SIO 硬件 FIFO 事件通知（READY/DONE + frame_id 校验），无锁无撕裂——
>   缓冲所有权随消息在两核间转移。
> - `ipc bench/selftest` 计时段为忙等（保证 µs 级计时精度），期间 Shell 短暂独占 core0。
> - **实测参考**（flash74，125MHz）：bench 16 帧全 ok，avg≈427µs/帧，双向吞吐≈75MB/s。
> - 诊断日志分级：正常启动静默；故障路径（STUCK/TIMEOUT/VERIFY FAIL）自动打 TTL；
>   全程分阶段日志用 `shmem_ipc.h` 的 `IPC_VERBOSE_DIAG=1` 重编开启。

### 2.7 展示与稳定性验证（v2.7 新增，开机自动运行）

| 命令 | 用法 | 说明 |
|---|---|---|
| `show` | `show status` | 稳定性报告：帧数/FPS(now,min,avg,max)/I2C 错误/运行时长 |
| | `show stop` | 本次停止（不写 Flash，下次开机仍自动运行） |
| | `show start` | 本次启动 |
| | `show on` | 固化"开机自动运行"（默认即开） |
| | `show off` | **固化删除**：开机不再自动运行（写入 Config 区） |

三展示要素（预设，无需输入任何指令）：
1. **I2C OLED 高速动画**（SSD1306 128×64 @0x3C，I2C0 GP4/5，400kHz）——
   场景每 5s 轮播：旋转线框立方体 → 双弹跳球 → 三重正弦示波器；
   每帧右上角实时显示刷新率 **FPS**。
2. **板载 LED 呼吸灯**（GPIO25，硬件 PWM 1kHz 载波）——占空比 0→100→0
   缓慢扫动（约 6.4s 周期），缓慢亮起、缓慢暗下循环。
3. **GP15 持续 PWM 输出**（硬件 PWM 1kHz @125MHz）——占空比与呼吸灯同步
   扫动，示波器可见呼吸包络的 PWM 波（超频后载波按主频等比升高）。

稳定性验证方法：让系统长跑（小时级），期间可随意使用 shell / IPC / MSC；
`show status` 的 fps min≈avg≈max、i2c_err 不增长、frames 持续累加，即证明
调度 / I2C / tick / 内存全链路稳定。

---

## 三、常用操作流程

1. **烧录**：`cd rp2040demo && .\build.bat`，把 `build/rp2040demo.uf2` 拖入 `RPI-RP2` 盘符。
2. **连接终端**：USB 串口（115200-8-N-1）查看启动 banner，进入 `mk> ` 交互式 Shell。
3. **查看系统**：`help` → `ps` → `heap` → `version`。
4. **初始化 I2C 并让机器记住**：`! i2c init 0 4 5 100000`（先执行并固化）。
5. **验证三任务调度**：`vtest start` → `vtest status` → `vtest stop`。
6. **超频并固化**：`ovclk try 250`（临时体验）→ `ovclk set 250` → `ovclk save` → `reboot`。
7. **多核**：`mcore demo` → `mcore status`。
8. **双核 IPC**：`ipc start` → `ipc selftest` → `ipc bench 64` → `ipc status`。
9. **展示/稳定性**：开机自动运行（OLED FPS + 呼吸灯 + GP15 PWM）；`show status` 看报告，`show off` 删除。
10. **U 盘文件**：`msc mount`（电脑读写）→ `msc eject` → `mkdir /DATA` → `ls`。

---

## 四、排错提示

| 现象 | 建议 |
|---|---|
| LED 爆闪 / 5Hz 快闪 | HardFault，检查任务栈大小、I2C 单事务原子性、栈帧布局 |
| 串口无输出 | 检查 COM 口号是否变化；Windows USB 驱动死锁时物理拔插 USB |
| U 盘看不到 | `msc mount`；若空盘先 `msc format` |
| 固化命令没生效 | `boot status` 查看回放结果；确认 `save` 顺序 |
| `ipc start` 失败 / selftest FAIL | `ipc status` 看 state/alive；alive=N 说明 Core1 卡死 → `ipc stop` 后重新 `ipc start`；确认没同时跑 `mcore demo`（互斥） |
| 想清除所有固化 | `factory_reset confirm` 或 `unsave all` |